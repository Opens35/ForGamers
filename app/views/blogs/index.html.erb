<div class="container">
  <p id="notice"><%= notice %></p>

  <h1>Blogs</h1>

  <table class="table table-striped">
    <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= blog.title %></td>
        <td><%= blog.review.truncate(60)  %></td>
        <td><%= link_to 'Show', blog %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

</div>